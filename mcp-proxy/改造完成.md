# mcp-proxy 改造完成

## 完成时间

2025-06-25

## 真实改动仓库

`../mcp-proxy`（Rust / Axum MCP 工具代理服务）

## 已完成改造

### 1. Dockerfile 端口对齐

- **文件**：`docker/Dockerfile.mcp-proxy`
- **改动**：EXPOSE 和健康检查端口从 `8080` 对齐为 `8089`（容器内端口）
- **健康检查**：`curl -f http://localhost:8089/health`

### 2. Docker 配置文件

- **文件**：`docker/config.yml`
- **改动**：默认端口为 `8089`，日志路径 `/app/logs`

### 3. 环境变量覆盖（已有）

- **文件**：`mcp-proxy/src/config.rs`
- **已有支持**：
  - `MCP_PROXY_PORT` — 覆盖服务端口
  - `MCP_PROXY_LOG_DIR` — 覆盖日志目录
  - `MCP_PROXY_LOG_LEVEL` — 覆盖日志级别
  - `RUST_LOG` — Rust 日志过滤级别

### 4. 构建与部署脚本

- **文件**：`scripts/build-runtime-image.sh` — Jenkins/本地统一构建镜像
- **文件**：`scripts/deploy-runtime-container.sh` — 单容器启动脚本

## 已修改文件清单

| 文件 | 改动类型 |
|------|----------|
| `docker/Dockerfile.mcp-proxy` | EXPOSE 和健康检查端口对齐 8089 |
| `docker/config.yml` | 默认端口 8089 |
| `scripts/build-runtime-image.sh` | 新增构建脚本 |
| `scripts/deploy-runtime-container.sh` | 新增部署脚本 |

## 部署参数

| 参数 | 值 |
|------|-----|
| 容器名 | `nuwax-mcp-proxy` |
| 镜像 | `mcp-proxy:local` |
| 容器内端口 | `8089` |
| 宿主机端口 | `8020` |
| 网络 | `bridge`（默认，不依赖 compose 网络） |
| 后端连接参数 | `MCP_PROXY_URL=http://host.docker.internal:8020` |
| 代码执行连接 | `CODE_EXECUTE_URL=http://host.docker.internal:8020/api/run_code_with_log` |

## 挂载目录

| 容器路径 | 宿主机路径 | 说明 |
|----------|-----------|------|
| `/app/config.yml` | `nuwax_deploy/docker/config/mcp_config.yml` | 配置文件（只读） |
| `/app/logs` | `nuwax_deploy/docker/logs/mcp_proxy` | 日志持久化 |
| `/root/.cache/uv` | `nuwax_deploy/docker/data/uv_cache/uv` | uv 包缓存 |
| `/root/.npm` | `nuwax_deploy/docker/data/npx_cache/.npm` | npm 包缓存 |

## 验证结果

### Docker 镜像构建

```bash
cd /Users/atan/Desktop/work/vs_code_nuwax/mcp-proxy
bash scripts/build-runtime-image.sh  # ✅ 成功，镜像 mcp-proxy:local
```

### 单容器启动

```bash
bash scripts/deploy-runtime-container.sh  # ✅ 成功
```

```
容器：nuwax-mcp-proxy
状态：Up (healthy) ✅
端口：0.0.0.0:8020->8089/tcp
网络：bridge ✅
```

### 接口验证

```bash
curl -fsS http://localhost:8020/health  # ✅ 返回 "health"
```

## 后续注意事项

1. **镜像较大**：runtime 阶段包含 Rust、Node、Python、Deno、uv，镜像较大，但有利于兼容动态 MCP 工具
2. **缓存目录**：uv 和 npm 缓存目录必须保留挂载，否则首次工具调用会变慢
3. **Deno/uv 安装**：依赖外网脚本，Jenkins 弱网环境可能失败，后续可沉淀内部基础镜像
4. **不执行 `docker compose down -v`**：不删除原 `data/uv_cache`、`data/npx_cache`、`logs/mcp_proxy` 目录
