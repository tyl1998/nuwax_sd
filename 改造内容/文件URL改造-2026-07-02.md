# 文件 URL 改造 - 2026-07-02

## 问题背景

通过 IP 地址访问平台时（如 `http://10.94.77.42:18080`），出现两个问题：

1. **CORS 403**：后端 `CORS_ALLOW_ORIGIN` 只配置了 `localhost`，未包含 IP 地址
2. **后端内部文件访问超时**：后端生成的文件 URL 使用 `siteUrl`（如 `http://10.94.77.42:18080`），后端自己用该 URL 发 HTTP 请求解析文档时，访问不到外部地址导致超时

## 改造方案

### 核心思路

- 文件 URL **仍然存储完整 URL**（前端知识库模块依赖 `url` 字段为完整 URL）
- 后端内部访问文件时，通过 `FileUrlResolver` 将外部地址转为 `http://localhost:{port}`，自己访问自己
- `FileUrlResolver` 对已经是绝对 URL 的请求，将 host 替换为 localhost；对相对路径直接拼接 localhost

### 新增文件

| 文件 | 说明 |
|------|------|
| `FileUrlResolver.java` | 统一 URL 解析工具，外部 URL → `http://localhost:{port}` 同路径 |

路径：`system-spec/src/main/java/com/xspaceagi/system/spec/utils/FileUrlResolver.java`

```java
public class FileUrlResolver {
    private static final String LOCAL_BASE;
    static {
        String port = System.getProperty("server.port", System.getenv().getOrDefault("APP_PORT", "8080"));
        LOCAL_BASE = "http://localhost:" + port;
    }
    public static String toAbsoluteUrl(String url) {
        if (url == null) return url;
        if (url.startsWith("/")) {
            return LOCAL_BASE + url;
        }
        // 替换外部地址的 host 为 localhost
        try {
            URI uri = new URI(url);
            URI localUri = new URI("http", null, "localhost", Integer.parseInt(LOCAL_BASE.substring(LOCAL_BASE.lastIndexOf(":") + 1)), uri.getPath(), uri.getQuery(), null);
            return localUri.toString();
        } catch (Exception e) {
            return url;
        }
    }
}
```

### 修改文件清单

| 文件 | 改动 |
|------|------|
| `FileManagementServiceImpl.java` | **不改**，保留 `siteUrl` 拼接完整 URL |
| `TikaParser.java` | 2 处 `new URL(docUrl)` 加 `FileUrlResolver.toAbsoluteUrl()` |
| `PdfSegmentationServiceNative.java` | 1 处 `new URL(pdfUrl)` 加 `FileUrlResolver.toAbsoluteUrl()` |
| `DocumentExtractNodeHandler.java` | 1 处 `new URL(url0)` 加 `FileUrlResolver.toAbsoluteUrl()` |
| `CustomPageChatFluxServiceImpl.java` | 1 处 `new URL(url0)` 加 `FileUrlResolver.toAbsoluteUrl()` |
| `AgentWorkspaceApplicationServiceImpl.java` | 1 处 `new URL(skillUrl)` 加 `FileUrlResolver.toAbsoluteUrl()` |
| `UrlFile.java` | `parseToString`、`downLoad`、`getConnectionStream` 3 处加 `FileUrlResolver.toAbsoluteUrl()` |
| `FileAccessServiceImpl.java` | `getUrlPath()` 支持相对路径直接返回（防御性改动） |

### 不改的场景

| 场景 | 原因 |
|------|------|
| `FileManagementServiceImpl` 存储的 `fileUrl` | 前端知识库模块依赖完整 URL |
| 返回前端的模型代理 URL、分享链接等 | 浏览器需要完整 URL |
| 飞书/钉钉回调、沙箱 OAuth 跳转 | 外部服务需要公网可达的 URL |

## 效果

- 前端拿到的文件 URL 仍是完整 URL，知识库上传流程不受影响
- 后端内部解析文档时用 `http://localhost:8080/api/f/xxx` 访问自己，不会超时
- 文件预览/下载前端走同源代理，自动适配任何访问地址
